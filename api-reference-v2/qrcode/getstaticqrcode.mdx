---
title: "Get Static QRCode"
openapi: "GET /endpointqrcodev2/getstaticqrcode"
description: "Endpoint to retrieve the merchant's static QRIS code"
---

{/*
  Notes for maintainers — REPRESENTATIVE response.
  GET /hl/v2/qr-codes/static returns the merchant's static QRIS image (response data { url }).
  Per master source (payme.getQrCode): when the account has no static QRIS configured the
  handler returns 404 { statusCode: 404, messages: "Static QR not configured" }; any other
  failure returns 500 { statusCode: 500, messages: <error> }. The 200 shape below is
  representative — confirm against a merchant that has a static QRIS enabled.
  Envelope key is PLURAL `messages` for this group (reads, writes, AND errors).
*/}

<RequestExample>

```bash Request Example
curl --request GET 'https://api.mayar.id/hl/v2/qr-codes/static' \
 --header 'Authorization: Bearer Paste-Your-API-Key-Here'
```

</RequestExample>

<ResponseExample>

```json Response Example 200
{
    "statusCode": 200,
    "messages": "success",
    "data": {
        "url": "https://media.mayar.id/images/resized/480/a1b2c3d4-e5f6-4789-a012-3456789abcde.png"
    }
}
```

```json Response Example 404
{
    "statusCode": 404,
    "messages": "Static QR not configured"
}
```

</ResponseExample>

Endpoint:

<CodeGroup>

```Production Production
https://api.mayar.id/hl/v2/qr-codes/static
```

```Sandbox Sandbox
https://api.mayar.club/hl/v2/qr-codes/static
```

</CodeGroup>

## Authorization

<ResponseField name="Authorization" type="string" required>
  Example:

  `Authorization | Bearer Paste-Your-API-Key-Here`
</ResponseField>

## Response

Successful Response

### Main Structure (Root)

<ResponseField name="statusCode" type="integer">
  Status code returned by the API.
</ResponseField>

<ResponseField name="messages" type="string">
  Status message that describes the status code.
</ResponseField>

<ResponseField name="data" type="object">
  The static QRIS object.
</ResponseField>

### data Structure (Object)

<ResponseField name="data.url" type="string">
  Link to the merchant's static QRIS image file.
</ResponseField>

## Errors

<ResponseField name="404 Static QR not configured" type="object">
  Returned when the merchant has no static QRIS configured: `{ "statusCode": 404, "messages": "Static QR not configured" }`.
</ResponseField>

<ResponseField name="500" type="object">
  Returned on any other failure while generating the QRIS image: `{ "statusCode": 500, "messages": "<error>" }`.
</ResponseField>
