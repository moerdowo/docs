---
title: "Look Up Membership Member"
openapi: "GET /endpointmembershipv2/productmember"
description: "Endpoint to look up a single membership member of a product by member ID."
---

{/*
  Notes for maintainers — REPRESENTATIVE read sample.
  Source: the v2 route GET /memberships/:uuId/members is backed by the legacy membership
  read handler (same handler the v1 GET /membership-member-detail/:uuId route uses). It
  addresses the product by path :uuId (= productId) and filters to a single member by the
  required `memberId` query parameter, scoped to the authenticated merchant.
  KEY DIFFERENCE vs "Get Membership Member Detail" (GET /memberships/members/:memberId):
  that page keys off memberId in the PATH and productId in the QUERY and returns NESTED
  `customer` / `paymentLink` / `membershipTier` objects. THIS legacy read keys off productId
  in the PATH and memberId in the QUERY, and FLATTENS the customer into top-level
  `customerEmail` / `customerName` / `customerMobile` fields (the nested `customer`, plus the
  internal `id` and `customerId`, are stripped from the payload). Envelope key is PLURAL
  `messages`. When no member matches, the body is `{ statusCode: 404, messages: "Not Found" }`.
  The membership-customer field set below is REPRESENTATIVE — confirm against a live response.
  Values are synthetic.
*/}

<RequestExample>

```bash Request Example
curl --request GET 'https://api.mayar.id/hl/v2/memberships/7c9d2e1f-4a5b-4c6d-8e9f-0a1b2c3d4e5f/members?memberId=MBR8X2QK' \
--header 'Authorization: Bearer Paste-Your-API-Key-Here'
```

</RequestExample>

<ResponseExample>

```json Response Example 200
{
    "statusCode": 200,
    "messages": "success",
    "data": {
        "customerEmail": "budi.santoso@example.com",
        "customerName": "Budi Santoso",
        "customerMobile": "081234567890",
        "createdAt": "2026-01-15T15:29:59.659Z",
        "expiredAt": "2026-02-15T15:29:59.430Z",
        "isAlreadyUsedTrial": true,
        "isInTrial": false,
        "isLifetimePeriod": null,
        "isTodayReminderSent": true,
        "memberId": "MBR8X2QK",
        "membershipTierId": "9b2d4f6a-8c1e-4a3b-bd5c-6e7f8a9b0c1d",
        "monthlyPaymentPeriod": null,
        "nextPayment": "2026-02-15T15:29:59.430Z",
        "nextPaymentEmailSent": true,
        "paymentLinkId": "7c9d2e1f-4a5b-4c6d-8e9f-0a1b2c3d4e5f",
        "status": "inactive",
        "updatedAt": "2026-02-15T17:30:03.531Z",
        "userId": "348e083d-315a-4e5c-96b1-5a2a98c48413"
    }
}
```

```json Response Example 404
{
    "statusCode": 404,
    "messages": "Not Found"
}
```

</ResponseExample>

Endpoint:

<CodeGroup>

```Production Production
https://api.mayar.id/hl/v2/memberships/(productId)/members?memberId=(memberId)
```

```Sandbox Sandbox
https://api.mayar.club/hl/v2/memberships/(productId)/members?memberId=(memberId)
```

</CodeGroup>

<Note>
  This endpoint is also reachable through the legacy singular-path alias
  `GET /hl/v2/membership-member-detail/(productId)` (the v1 path, now mirrored under
  `/hl/v2`). Both paths share the same handler, authorization, and response. Prefer the
  canonical `/hl/v2/memberships/(productId)/members` form shown above.
</Note>

## Authorization

<ResponseField name="Authorization" type="string" required>
  Example:

  `Authorization | Bearer Paste-Your-API-Key-Here`
</ResponseField>

## Path Parameters

<ParamField path="productId" type="string" required>
  The unique identifier of the membership product the member belongs to.

  Example:

  `7c9d2e1f-4a5b-4c6d-8e9f-0a1b2c3d4e5f`
</ParamField>

## Query Parameters

<ParamField query="memberId" path="memberId" type="string" required>
  The public membership code of the member to look up. The endpoint returns the single
  matching member for the product.

  Example:

  `MBR8X2QK`
</ParamField>

## Response

Successful Response

### Main Structure (Root)

<ResponseField name="statusCode" type="integer" >
  Status code form API.
</ResponseField>

<ResponseField name="messages" type="string" >
  Status message that describes the status code.
</ResponseField>

<ResponseField name="data" type="object">
  The membership member record. The customer is flattened into the top-level
  `customerEmail`, `customerName`, and `customerMobile` fields rather than a nested
  `customer` object.
</ResponseField>

### data Structure (Object)

<ResponseField name="customerEmail" type="string" >
  Email address of the member's customer.
</ResponseField>

<ResponseField name="customerName" type="string" >
  Name of the member's customer.
</ResponseField>

<ResponseField name="customerMobile" type="string" >
  Mobile number of the member's customer.
</ResponseField>

<ResponseField name="createdAt" type="string" >
  ISO 8601 timestamp (UTC) of when the member was created.
</ResponseField>

<ResponseField name="expiredAt" type="string | null" >
  ISO 8601 timestamp (UTC) of when the membership expires, normalized to ISO 8601.
</ResponseField>

<ResponseField name="isAlreadyUsedTrial" type="boolean" >
  Whether the member has already used the trial.
</ResponseField>

<ResponseField name="isInTrial" type="boolean" >
  Whether the member is currently in a trial period.
</ResponseField>

<ResponseField name="isLifetimePeriod" type="boolean | null" >
  Whether the member is on a lifetime period.
</ResponseField>

<ResponseField name="isTodayReminderSent" type="boolean" >
  Whether today's payment reminder has been sent.
</ResponseField>

<ResponseField name="memberId" type="string" >
  Public membership code of the member.
</ResponseField>

<ResponseField name="membershipTierId" type="string<uuid>" >
  Unique identifier of the member's tier.
</ResponseField>

<ResponseField name="monthlyPaymentPeriod" type="integer | null" >
  The member's monthly payment period, or `null` when not set.
</ResponseField>

<ResponseField name="nextPayment" type="string | null" >
  ISO 8601 timestamp (UTC) of the member's next scheduled payment, normalized to ISO 8601.
</ResponseField>

<ResponseField name="nextPaymentEmailSent" type="boolean" >
  Whether the next-payment email has been sent.
</ResponseField>

<ResponseField name="paymentLinkId" type="string<uuid>" >
  Unique identifier of the product (payment link) the member belongs to.
</ResponseField>

<ResponseField name="status" type="string" >
  Current status of the member (for example `active`, `inactive`).
</ResponseField>

<ResponseField name="updatedAt" type="string" >
  ISO 8601 timestamp (UTC) of the last update.
</ResponseField>

<ResponseField name="userId" type="string<uuid>" >
  The user ID of the owner of the product.
</ResponseField>

## Errors

<ResponseField name="404 Not Found" type="object">
  Returned with the plural `messages` key when no member matches the given `memberId`
  for the product: `{ "statusCode": 404, "messages": "Not Found" }`. Note this read
  uses the plural `messages` key on both the success and the not-found body.
</ResponseField>
