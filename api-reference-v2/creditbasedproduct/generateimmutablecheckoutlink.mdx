---
title: "Generate Immutable Checkout Link"
openapi: "POST /endpointcreditbasedproductv2/generateimmutablecheckoutlink"
description: "Endpoint to generate an immutable checkout link for a credit based product."
---

{/*
  Notes for maintainers — REPRESENTATIVE / synthetic sample.
  Credit Based Product endpoints are served by a SEPARATE service mounted at /credit/v1 — they are
  NOT part of the /hl/v2 surface, so the real base URL is kept (no fabricated /hl/v2 path). The
  envelope is DATA-WRAPPED and uses the SINGULAR `message` key on success:
  { statusCode, message, data:{...} }. This is the PRODUCT variant (credit_usage_product), keyed by
  productId + customerInfo + creditAmount. The `data` object fields shown are REPRESENTATIVE.
  Authorization is a Bearer API key. Example ids, urls and customer info are synthetic.
*/}

<RequestExample>

```bash Request Example
curl --request POST 'https://api.mayar.id/credit/v1/credit/generate/immutable/checkout' \
--header 'Authorization: Bearer Paste-Your-API-Key-Here' \
--header 'Content-Type: application/json' \
--data '{
    "productId": "0e787345-9aaf-4b72-aa00-1174be6b52b1",
    "creditAmount": 1000,
    "customerInfo": {
        "name": "Budi Santoso",
        "email": "budi.santoso@example.com",
        "mobile": "081234567890"
    }
}'
```

</RequestExample>

<ResponseExample>

```json Response Example 200
{
    "statusCode": 200,
    "message": "success",
    "data": {
        "creditUsageImmutableCheckoutUrl": "https://testingmayar.myr.id/pl/test-product-credit?immutable=eyJuYW1lIjoiQnVkaSBTYW50b3NvIiwiZW1haWwiOiJidWRpLnNhbnRvc29AZXhhbXBsZS5jb20ifQ==.03de9bf29c5861d950d13364c2c23d62675cd2f03cd27a5b7c682f2c43d25b4e",
        "paymentLinkUrl": "https://testingmayar.myr.id/pl/test-product-credit"
    }
}
```


```json Response Example 400
{
    "statusCode": 400,
    "message": "You are not authorized to edit this product!"
}
```

</ResponseExample>

Endpoint:

<CodeGroup>

```Production Production
https://api.mayar.id/credit/v1/credit/generate/immutable/checkout
```


```Sandbox Sandbox
https://api.mayar.club/credit/v1/credit/generate/immutable/checkout
```

</CodeGroup>

## Authorization

<ResponseField name="Authorization" type="string" required>
  Example:

  `Authorization | Bearer Paste-Your-API-Key-Here`
</ResponseField>

## Request Body

<ParamField body="productId" type="string" required>
  The unique identifier of the product to generate the checkout link for.
</ParamField>

<ParamField body="creditAmount" type="number">
  The credit amount for the checkout. Only applicable for credit usage products.
</ParamField>

<ParamField body="customerInfo" type="object" required>
  Object containing the customer's details.
</ParamField>

### customerInfo Structure (Object)

<ParamField body="customerInfo.name" type="string" required>
  Full name of the customer.
</ParamField>

<ParamField body="customerInfo.email" type="string" required>
  Email address of the customer.
</ParamField>

<ParamField body="customerInfo.mobile" type="string" required>
  Mobile phone number of the customer.
</ParamField>

## Response

Successful Response

<Note>
  The `data` object fields shown below are representative. They contain the
  generated immutable checkout URL and the general payment link URL.
</Note>

### Main Structure (Root)

<ResponseField name="statusCode" type="integer">
  Status code from API.
</ResponseField>

<ResponseField name="message" type="string">
  Status message that describes the status code (`"success"` on success).
</ResponseField>

<ResponseField name="data" type="object">
  Object containing the generated checkout links.
</ResponseField>

### data Structure (Object)

<ResponseField name="creditUsageImmutableCheckoutUrl" type="string">
  A unique immutable checkout URL containing the encoded customer identity fields
  and `creditAmount`.
</ResponseField>

<ResponseField name="paymentLinkUrl" type="string">
  The general (mutable) payment link URL for the product.
</ResponseField>

### Errors

<ResponseField name="400 Not Authorized" type="object">
  Returned when the authenticated user does not own the product:
  `{ "statusCode": 400, "message": "You are not authorized to edit this product!" }`.
</ResponseField>

<ResponseField name="400 Validation Error" type="object">
  Returned when a required body parameter is missing or invalid. Validation errors
  use the PLURAL `messages` key with the error details in `data`:
  `{ "statusCode": 400, "messages": "Validation Error", "data": [ ... ] }`.
</ResponseField>

<ResponseField name="500 Internal Server Error" type="object">
  Returned on an unexpected server error:
  `{ "statusCode": 500, "message": "..." }`.
</ResponseField>
