---
title: "Edit Single Payment Request"
openapi: "POST /endpointreqpaymentv2/edit"
description: "Endpoint used to edit a single payment request that has been created"
---

{/* Notes for maintainers: this is a WRITE endpoint. The response shape below is
REPRESENTATIVE — derived from the V2 source handler (requestPayment.editPayment),
not captured from a live prod write. IMPORTANT: the payment request to update is
selected by the `{uuId}` PATH segment (req.params.uuId) — the handler loads the
payment request by that id and checks ownership against it. There is NO `id` body
field (the REQUEST_EDIT schema does not define one). `amount` is the only required
body field. `messages` is PLURAL on this endpoint. */}

<RequestExample>

```bash Request Example
curl --request POST 'https://api.mayar.id/hl/v2/payments/6f8c19ff-5b97-4792-aa89-d2a12797b356/update' \
--header 'Authorization: Bearer Paste-Your-API-Key-Here' \
--data-raw '{
        "amount": 150000,
        "description": "Penagihan yang sudah diedit",
        "expiredAt": "2026-07-19T16:43:23.000Z"
    }'
```

</RequestExample>

<ResponseExample>

```json Response Example 200
{
    "statusCode": 200,
    "messages": "success",
    "data": {
        "id": "6f8c19ff-5b97-4792-aa89-d2a12797b356",
        "transactionId": null,
        "link": "https://testingmayar.myr.id/pl/rqp9x2k7m3a/",
        "amount": 150000,
        "description": "Penagihan yang sudah diedit",
        "notes": null,
        "status": "unpaid",
        "expiredAt": "2026-07-19T16:43:23.000Z",
        "updatedAt": "2026-07-03T09:15:00.000Z",
        "customer": null
    }
}
```

</ResponseExample>

Endpoint:

<CodeGroup>

```Production Production
https://api.mayar.id/hl/v2/payments/{uuId}/update
```


```Sandbox Sandbox
https://api.mayar.club/hl/v2/payments/{uuId}/update
```

</CodeGroup>

<Note>
  The payment request to update is selected by the `{uuId}` segment in the URL
  **path** — send the target payment request id there. The request body carries
  only the fields you want to change (`amount` is required).
</Note>

## Authorization

<ResponseField name="Authorization" type="string" required>
  Example:

  `Authorization | Bearer Paste-Your-API-Key-Here`
</ResponseField>

## Path Parameters

<ParamField path="uuId" type="string<uuid>" required>
  The id of the payment request to update. This is the value returned as `id` when
  the payment request was created.

  Example:

  `6f8c19ff-5b97-4792-aa89-d2a12797b356`
</ParamField>

## Request Body

<ParamField path="amount" type="integer" required>
  Nominal amount of payment. Must be a positive integer of at least `500`. This is
  the only required body field.
</ParamField>

<ParamField path="email" type="string">
  Customer email. When present, a customer record and a transaction are created/
  linked and a `transactionId` is returned.
</ParamField>

<ParamField path="mobile" type="string">
  Customer telephone number.
</ParamField>

<ParamField path="description" type="string">
  General description of the transaction or order (HTML tags are stripped).
</ParamField>

<ParamField path="notes" type="string">
  Additional notes for the payment request.
</ParamField>

<ParamField path="expiredAt" type="string (format ISO 8601 date-time)">
  The transaction expiration date and time. After this time, the payment link is
  no longer valid.
</ParamField>

<ParamField path="paymentMethod" type="string">
  Restrict the payment request to a specific payment method.
</ParamField>

<ParamField path="cashtag" type="string">
  Cashtag associated with the payment request.
</ParamField>

<ParamField path="extraData" type="object">
  Additional custom data attached to the payment request (merged with any existing
  extra data).
</ParamField>

## Response

Successful Response

### Main Structure (Root)

<ResponseField name="statusCode" type="integer" >
  Status code form API.
</ResponseField>

<ResponseField name="messages" type="string" >
  Status message that describes the status code.
</ResponseField>

<ResponseField name="data" type="object">
  The updated payment request data.
</ResponseField>

### data Structure (Object)

<ResponseField name="id" type="string<uuid>" >
  Unique payment request id (echoes the `{uuId}` path segment).
</ResponseField>

<ResponseField name="transactionId" type="string<uuid> | null" >
  Transaction id linked to this payment request. `null` when no customer email is
  associated.
</ResponseField>

<ResponseField name="link" type="string" >
  The URL that customers can access to open the payment request.
</ResponseField>

<ResponseField name="amount" type="integer" >
  The updated payment amount.
</ResponseField>

<ResponseField name="description" type="string | null" >
  Payment request description.
</ResponseField>

<ResponseField name="notes" type="string | null" >
  Additional notes attached to the payment request.
</ResponseField>

<ResponseField name="status" type="string" >
  Payment request status (e.g. `unpaid`, `paid`, `closed`).
</ResponseField>

<ResponseField name="expiredAt" type="string (ISO 8601) | null" >
  Expiration date-time as an ISO 8601 string, or `null`.
</ResponseField>

<ResponseField name="updatedAt" type="string (ISO 8601)" >
  Last-updated date-time as an ISO 8601 string.
</ResponseField>

<ResponseField name="customer" type="object | null" >
  Associated customer object, or `null` when the payment request has no customer.
</ResponseField>

### Errors

<ResponseField name="400 Bad Request" type="object">
  Returned when the request body fails validation (e.g. missing `amount`):
  `{ "statusCode": 400, "messages": "Validation Error" }`.
</ResponseField>

<ResponseField name="401 Unauthorized" type="object">
  Returned when the payment request identified by `{uuId}` does not belong to the
  authenticated account:
  `{ "statusCode": 401, "messages": "Unauthorized" }`.
</ResponseField>

<ResponseField name="404 Not Found" type="object">
  Returned when no payment request matches the `{uuId}` path segment:
  `{ "statusCode": 404, "messages": "Payment not found" }`.
</ResponseField>

<ResponseField name="409 Conflict" type="object">
  Returned when the payment request has already been paid and can no longer be
  edited:
  `{ "statusCode": 409, "messages": "Transaction already paid. Cannot edit payment request." }`.
</ResponseField>
