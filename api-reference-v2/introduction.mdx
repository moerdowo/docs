---
title: "Introduction"
description: "Section for showcasing API V2 endpoints"
---

## Get Started

Headless API is an e-commerce development method where the user interface (Frontend) is separated from the product management and transaction processing systems (Backend). In headless commerce, the front-end can be built using various modern web technologies such as React or Vue.js, while the back-end performs tasks such as product storage, order processing, and integration with payment systems.

By separating the front-end and back-end, headless commerce allows development teams to focus on the user experience without being hampered by the limitations of the underlying e-commerce platform. It also helps reduce the cost and time required to update and maintain an e-commerce site.

This section documents the **V2** of the Mayar Headless API. V2 keeps the same authentication model as V1 but standardizes every response around a single envelope, described below.

## Authentication

For starting using Mayar Headless API, you need account at https://mayar.id. If you have an account, you can create API KEY from https://web.mayar.id/api-keys.

There are 2 options when creating an API Key, **Read Only** and **Read & Write**.

If you choose **Read Only**, then only API Endpoints with the Get Method can be used. If you choose **Read & Write**, then API Endpoints with both Get and Post Methods can be used.

![Generate API Key](/images/apikeynew.png)

Every request must include your API Key as a Bearer token in the `Authorization` header:

<CodeGroup>

```bash Authorization Header
Authorization: Bearer Paste-Your-API-Key-Here
```

</CodeGroup>

<Warning>
  For your account security, don't share your API KEY\!
</Warning>

<Note>
  Please note that any changes to your domain or subdomain require you to create a new API KEY, so the link in the response you receive will also be updated.
</Note>

## Sandbox Environment

For testing purposes, you can use the webpage https://web.mayar.club/. You can log in or register first, then generate an API key from https://web.mayar.club/api-keys. (The same method as obtaining an API key on the production page).

## Base URL

The API Base URL is the main part of the address (URL) used to access an API endpoint. Mayar has two API Base URL's based on the page.

<CodeGroup>

```bash Production
https://api.mayar.id/hl/v2
```


```bash Sandbox
https://api.mayar.club/hl/v2
```

</CodeGroup>

<Note>
  Make sure you use a base URL based on the appropriate web page (Production or Sandbox)
</Note>

## Response Envelope

Every V2 endpoint returns its result wrapped in a consistent envelope so clients can parse responses the same way across the whole API.

<CodeGroup>

```json Response Envelope
{
  "statusCode": 200,
  "messages": "success",
  "data": {}
}
```

</CodeGroup>

<ResponseField name="statusCode" type="integer">
  The HTTP status code of the request result. See [Status Code](/api-reference-v2/statuscode).
</ResponseField>

<ResponseField name="messages" type="string">
  A short outcome message for the request, for example `success` or `failed`. Some write endpoints return this field as `message` (singular) — refer to each endpoint page for its exact shape.
</ResponseField>

<ResponseField name="data" type="object">
  The payload of the response. Its shape depends on the endpoint. For list endpoints `data` is an array, and pagination is provided through cursor fields alongside it.
</ResponseField>

## Legacy path aliases

Every documented V2 endpoint uses a RESTful, plural-noun path (for example
`/hl/v2/products`, `/hl/v2/customers`, `/hl/v2/invoices`). For backward compatibility,
the original **V1 singular-path** routes are also mounted under `/hl/v2` and resolve to
the **same controllers, authorization, and response envelope** as their canonical V2
counterpart. The RESTful V2 routes always take precedence; the legacy paths act as a
fallback for any `/hl/v2` path a RESTful route does not already define.

<Note>
  These aliases exist for compatibility only. New integrations should call the canonical
  RESTful paths documented on each endpoint page. The table below maps every legacy alias
  to the page that documents its behavior. Unless flagged, the alias is identical to its
  canonical twin (same parameters, same `messages` envelope).
</Note>

| Legacy alias (under `/hl/v2`) | Canonical V2 endpoint |
| --- | --- |
| `GET /product` | `GET /products` |
| `GET /product/type/{type}` | `GET /products/types/{type}` |
| `GET /product/{action}/{uuId}` ¹ | `POST /products/{uuId}/{action}` |
| `GET /product/{uuId}` | `GET /products/{uuId}` |
| `POST /product` | `POST /products/create` |
| `POST /product/paymentlink/create` | `POST /products/payment-link/create` |
| `POST /product/webinar/create` | `POST /products/webinar/create` |
| `POST /product/event/create` | `POST /products/event/create` |
| `POST /product/digital-product/create` | `POST /products/digital-product/create` |
| `POST /product/paymentlink/edit` ² | `POST /products/payment-link/{uuId}/update` |
| `POST /product/webinar/edit` ² | `POST /products/webinar/{uuId}/update` |
| `POST /product/event/edit` ² | `POST /products/event/{uuId}/update` |
| `POST /product/digital-product/edit` ² | `POST /products/digital-product/{uuId}/update` |
| `POST /paymentlink/edit` ² | `POST /payment-links/{uuId}/update` |
| `POST /paymentlink/sort/{type}` | `POST /payment-links/sort/{type}` |
| `GET /invoice` | `GET /invoices` |
| `GET /invoice/filter` | `GET /invoices/filter` |
| `GET /invoice/{uuId}` | `GET /invoices/{uuId}` |
| `GET /invoice/{action}/{uuId}` ³ | `POST /products/{uuId}/{action}` (Change Invoice Status) |
| `POST /invoice/create` | `POST /invoices/create` |
| `POST /invoice/edit` ² | `POST /invoices/{uuId}/update` |
| `GET /payment` | `GET /payments` |
| `GET /payment/{uuId}` | `GET /payments/{uuId}` |
| `GET /payment/{action}/{uuId}` ¹ | `POST /payments/{uuId}/{action}` |
| `POST /payment/create` | `POST /payments/create` |
| `POST /payment/edit` ² | `POST /payments/{uuId}/update` |
| `GET /customer` | `GET /customers` |
| `GET /customer/detail` | `GET /customers/detail` |
| `POST /customer/create` | `POST /customers/create` |
| `POST /customer/update` ⁴ | `POST /customers/{uuId}/update` |
| `POST /customer/login/portal` | `POST /customers/portal-login` |
| `GET /membership/{uuId}` | `GET /memberships/{uuId}/tiers` (Get Membership Product Tiers) |
| `GET /membership-member-detail/{uuId}` | `GET /memberships/{uuId}/members` (Look Up Membership Member) |
| `GET /installment` | `GET /installments` |
| `GET /installment/{uuId}` | `GET /installments/{uuId}` |
| `POST /installment/create` | `POST /installments/create` |
| `POST /coupon/create` | `POST /coupons/create` |
| `GET /coupon/{uuId}` | `GET /coupons/{uuId}` |
| `POST /coupon/validate` | `POST /coupons/validate` |
| `POST /coupon/check` | `POST /coupons/check` |
| `POST /nameservice/create` | `POST /name-services/create` |
| `POST /nameservice/edit` ² | `POST /name-services/update` |
| `POST /qrcode/create` | `POST /qr-codes/create` |
| `GET /qrcode/static` | `GET /qr-codes/static` |
| `GET /balance` | `GET /balances` |
| `GET /webhook/history` | `GET /webhooks/history` |
| `POST /webhook/register` ⁵ | `POST /webhooks/update` |
| `POST /webhook/test` | `POST /webhooks/test` |
| `POST /webhook/retry` | `POST /webhooks/retry` |
| `GET /reviews/product/{paymentLinkId}` | `GET /products/{paymentLinkId}/reviews` |
| `GET /reviews/product/{paymentLinkId}/stats` | `GET /products/{paymentLinkId}/reviews/stats` |
| `GET /reviews/customer/{customerId}` ⁶ | `GET /products/{paymentLinkId}/reviews/customer` |
| `GET /reviews/merchant/{userId}/stats` ⁶ | `GET /reviews/stats` |
| `POST /reviews/edit` ² | `POST /reviews/{uuId}/update` |
| `POST /reviews/bulk-status` | `POST /reviews/bulk-status/update` |

<Warning>
  A few aliases differ from their canonical twin by more than the path — use the canonical
  V2 endpoint for predictable behavior:

  - **¹ Method differs** — the legacy product/payment status toggles are `GET` with an
    action-first path (`/{action}/{uuId}`); the canonical V2 form is `POST` with an
    id-first path (`/{uuId}/{action}`).
  - **² Edit by body `id`** — like their canonical twins, these update aliases read the
    resource `id` from the request **body**, not from the `{uuId}` path segment.
  - **³ Invoice status toggle** — the legacy `GET /invoice/{action}/{uuId}` exposes an
    invoice open/close toggle. The RESTful V2 router has no dedicated invoice-status route;
    the same capability is documented on the **Change Invoice Status** page via the shared
    product status toggle (`POST /products/{uuId}/{action}`).
  - **⁴ Addressing differs** — `POST /customer/update` is body-only (the customer is
    identified by `fromEmail` / `toEmail` in the body); the canonical form adds a `{uuId}`
    path segment.
  - **⁵ Different handler** — `POST /webhook/register` and the canonical `POST /webhooks/update`
    register a webhook URL but are backed by different handlers, so validation and behavior
    may differ. Prefer the canonical `POST /webhooks/update`.
  - **⁶ Addressing differs** — the legacy review lookups take `customerId` / `userId` in the
    **path**; the canonical forms use a query `customerId` under a product path and derive the
    merchant from the API key, respectively.
</Warning>

<Note>
  The `messages` envelope applies to these aliases exactly as to their canonical twins:
  plural `messages` for reads, writes, and errors — with the single exception of the
  `429 "Duplicate request detected. Please wait 1 minute before trying again."` response on
  `POST /invoice/create` and `POST /payment/create`, which uses the singular `message` key.
</Note>
