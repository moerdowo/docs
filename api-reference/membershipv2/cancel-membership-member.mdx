---
title: "Cancel Membership Member"
openapi: "POST /endpointmembershipv2/cancel-membership-member"
description: "Endpoint to cancel a membership member (sets status to stopped)."
---

<RequestExample>

```bash Request Example (productId in body)
curl --request POST 'https://api.mayar.id/hl/v2/membership-members/PUYSW40N/cancel' \
--header 'Authorization: Bearer Paste-Your-API-Key-Here' \
--header 'Content-Type: application/json' \
--data-raw '{
    "productId": "f5103e7c-9e30-467b-bc32-a96fdcb65849"
}'
```

```bash Request Example (productId in query)
curl --request POST 'https://api.mayar.id/hl/v2/membership-members/PUYSW40N/cancel?productId=f5103e7c-9e30-467b-bc32-a96fdcb65849' \
--header 'Authorization: Bearer Paste-Your-API-Key-Here'
```

</RequestExample>

<ResponseExample>

```json Response Example 200
{
    "message": "success",
    "statusCode": 200,
    "data": {
        "membershipCustomer": {
            "id": "9b3a1c7e-4d62-4f0a-8b21-2e9c5d7f3a10",
            "memberId": "PUYSW40N",
            "userId": "8f7e6d5c-4b3a-2918-0716-5a4b3c2d1e0f",
            "customerId": "c1f2a3b4-5d6e-7f80-91a2-b3c4d5e6f708",
            "membershipTierId": "137f0fa9-8aa5-4fec-947e-6ef223590861",
            "paymentLinkId": "f5103e7c-9e30-467b-bc32-a96fdcb65849",
            "monthlyPaymentPeriod": 1,
            "status": "stopped",
            "nextPayment": "2026-07-20T09:10:57.994Z",
            "expiredAt": null,
            "createdAt": "2026-06-20T09:10:57.994Z",
            "updatedAt": "2026-06-23T10:30:00.000Z"
        }
    }
}
```

</ResponseExample>

{/*
  Notes for maintainers (confirmed against the V2 membership API):
  - Success response is { message, statusCode, data } where data is
    { membershipCustomer: {…} }. Note the key ORDER (message, then statusCode).
  - Response uses the SINGULAR `message` key (writes use `message`; reads use plural `messages`). Do NOT normalize.
  - `productId` is accepted from the request BODY or the QUERY string (like the invoice endpoint). Missing
    from both responds 400 "Required: productId, memberId".
  - Cancelling flips the member's status to "stopped", but GUARDS against members
    that are already inactive — if status is already `stopped` or `finished` it responds
    400 "Membership member ini sudah tidak aktif (status: <status>)." (the <status> is interpolated).
  - The `data.membershipCustomer` FIELD NAMES/VALUES above are representative — confirm the exact field list
    against a live response if an exhaustive table is needed.
*/}

Endpoint:

<CodeGroup>

```Production Production
https://api.mayar.id/hl/v2/membership-members/(memberId)/cancel
```

```Sandbox Sandbox
https://api.mayar.club/hl/v2/membership-members/(memberId)/cancel
```

</CodeGroup>

## Authorization

This endpoint requires authentication — send your API key as a Bearer token.

<ResponseField name="Authorization" type="string" required>
  Example:

  `Authorization | Bearer Paste-Your-API-Key-Here`
</ResponseField>

## Path Parameters

<ParamField path="memberId" type="string" required>
  The public membership code of the member to cancel (for example `PUYSW40N`).

  Example:

  `PUYSW40N`
</ParamField>

## Request Body

`productId` may be supplied **in the request body OR as a query parameter** (see [Query Parameters](#query-parameters)). At least one location is required — if it is missing from both, the endpoint responds `400 "Required: productId, memberId"`.

<ParamField body="productId" type="string" required>
  The unique identifier of the product the member belongs to. Accepted here in the JSON body, or alternatively as a query parameter.

  Example:

  `f5103e7c-9e30-467b-bc32-a96fdcb65849`
</ParamField>

## Query Parameters

<ParamField query="productId" type="string">
  Alternative location for `productId` when you prefer not to send a request body. Use either the body field or this query parameter.

  Example:

  `?productId=f5103e7c-9e30-467b-bc32-a96fdcb65849`
</ParamField>

## Response

Successful Response — HTTP `200`

### Main Structure (Root Object)

<ResponseField name="message" type="string">
  Status message that describes the result. Note the **singular** `message` key (write endpoints use
  `message`; read/list endpoints use the plural `messages`).
</ResponseField>

<ResponseField name="statusCode" type="integer">
  Status code from the API. `200` on success.
</ResponseField>

<ResponseField name="data" type="object">
  The cancellation result wrapper.
</ResponseField>

<ResponseField name="data.membershipCustomer" type="object">
  The cancelled membership customer record. Its `status` is now `stopped`.
</ResponseField>

## Errors

All errors use the envelope `{ "statusCode": <code>, "message": "<reason>" }` (singular `message`).

```json Error Example 400
{
    "statusCode": 400,
    "message": "Membership member ini sudah tidak aktif (status: stopped)."
}
```

<ResponseField name="statusCode" type="integer">
  The HTTP status code of the error.
</ResponseField>

<ResponseField name="message" type="string">
  A human-readable description of the error.
</ResponseField>

Common errors:

| Status | `message` | When |
| --- | --- | --- |
| `400` | `Required: productId, memberId` | `productId` missing from both body and query, or `memberId` missing from the path. |
| `400` | `Membership member ini sudah tidak aktif (status: <status>).` | The member is already inactive — its status is already `stopped` or `finished` (the value is interpolated into the message). |
| `404` | `Member dengan ID <memberId> tidak ditemukan.` | No membership member matches the given `memberId`. |
