---
title: "Update Membership Member"
openapi: "PUT /endpointmembershipv2/update-membership-member"
description: "Endpoint to update an existing membership member."
---

{/* STUB — Membership V2. Section skeleton only; content filled in a later phase (plan phase 5). */}

<RequestExample>

```bash Request Example
curl --request PUT 'https://api.mayar.id/hl/v2/membership-members/MEMBER-ID' \
--header 'Authorization: Bearer Paste-Your-API-Key-Here' \
--data-raw '{}'
```

</RequestExample>

<ResponseExample>

```json Response Example 200
{
    "statusCode": 200,
    "message": "success",
    "data": {
        "membershipCustomer": {}
    }
}
```

</ResponseExample>

Endpoint:

<CodeGroup>

```Production Production
https://api.mayar.id/hl/v2/membership-members/(memberId)
```

```Sandbox Sandbox
https://api.mayar.club/hl/v2/membership-members/(memberId)
```

</CodeGroup>

## Authorization

<ResponseField name="Authorization" type="string" required>
  Example:

  `Authorization | Bearer Paste-Your-API-Key-Here`
</ResponseField>

## Path Parameters

{/* TODO: document memberId */}

<ParamField path="memberId" type="string" required>
  The unique identifier of the membership member.
</ParamField>

## Request Body

{/* TODO: document productId (required), membershipTierId, membershipMonthlyPeriod, status, nextPayment, expiredAt */}

<ParamField body="productId" type="string" required>
  The unique identifier of the product.
</ParamField>

## Response

Successful Response

### Main Structure (Root Object)

{/* TODO: document data.membershipCustomer fields */}

<ResponseField name="statusCode" type="integer">
  Status code from the API.
</ResponseField>

<ResponseField name="message" type="string">
  Status message that describes the status code.
</ResponseField>

<ResponseField name="data" type="object">
  The updated membership member.
</ResponseField>

<ResponseField name="data.membershipCustomer" type="object">
  Details of the updated membership customer.
</ResponseField>
